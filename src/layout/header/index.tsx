import { Link } from "react-router-dom";

const Header = () => {
    return (
        <header>
            <Link to="/signUp">
                signup
            </Link>
        </header>
    );
}

export default Header;
